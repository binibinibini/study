- tip ~ total_bill 상관 관계를 산점도로 시각화<br>
- 성별에 따라서 마커의 색깔을 다르게 표시.<br>

```python
male_df = tips[tips.sex == 'Male']
female_df = tips[tips.sex == 'Female']

plt.scatter(x = male_df.total_bill, y = male_df.tip, alpha = 0.3, label = 'Male', c = 'DodgerBlue')
plt.scatter(x = female_df.total_bill, y = female_df.tip, alpha = 0.3, label = 'Female', c = 'IndianRed')

plt.xlabel('Total bill')
plt.ylabel('Tip')
plt.legend()    # 범례 표시 - 각각의 산점도의 레이블을 표시
plt.grid()

plt.show()
```
![image](https://github.com/user-attachments/assets/36ff2808-162e-4c87-b219-9c08d49e5851)

<br>
- tip ~ total_bill 산점도<br>
- 성별에 따라서 마커의 색깔을 다르게 지정.<br>
- 흡연여부에 따라서 마커의 모양을 다르게 지정.<br>

```python
male_smoker = tips[(tips.sex == 'Male') & (tips.smoker == 'Yes')]
male_nonsmoker = tips[(tips.sex == 'Male') & (tips.smoker == 'No')]
female_smoker = tips[(tips.sex == 'Female') & (tips.smoker == 'Yes')]
female_nonsmoker = tips[(tips.sex == 'Female') & (tips.smoker == 'No')]


plt.scatter(x = male_smoker.total_bill, y = male_smoker.tip,
            alpha = 0.4,
            c = 'blue',
            marker = 'o',
            label = 'Male-Smoker')

plt.scatter(x = male_nonsmoker.total_bill, y = male_nonsmoker.tip, 
            alpha = 0.4,
            c = 'blue', 
            marker = '^',
            label = 'Male-NonSmoker')

plt.scatter(x = female_smoker.total_bill, y = female_smoker.tip,
            c = 'red', 
            alpha = 0.4, 
            marker = 'o',
            label = 'Female-Smoker')

plt.scatter(x = female_nonsmoker.total_bill, y = female_nonsmoker.tip,
            c = 'red',
            alpha = 0.4,
            marker = '^',
            label = 'Female-NonSmoker')


plt.xlabel('Total bill')
plt.ylabel('Tip')
plt.legend()    # 각각의 scatter 함수의 label을 범례 표시.
plt.grid()

plt.show()
```
![image](https://github.com/user-attachments/assets/d735a64d-32f7-4e6b-87cb-283e5dffcb13)

```python
gender = tips.sex.cat.categories    # tips.sex.unique()
smoker = tips.smoker.cat.categories
colors = ['DodgerBlue', 'IndianRed']    # 성별에 따라서 다르게 표시할 색깔 이름
markers = ['s', '^']  # 흡연여부에 따라서 다르게 표시할 마커 모양

for g, c in zip(gender, colors):
    for s, m in zip(smoker, markers):
        subset_df = tips[(tips.sex == g) & (tips.smoker == s)]
        plt.scatter(x = subset_df.total_bill, y = subset_df.tip,
                    alpha = 0.4, c = c, marker = m,
                    label = f'{g} Smoker-{s})

plt.legend()
plt.xlabel('Total bill')
plt.ylabel('Tip')
plt.grid()

plt.show()
```
![image](https://github.com/user-attachments/assets/6c64ffae-a291-4ec3-bdd0-9ec376085f5f)

<br>

# Line plot(선 그래프)
주로 시계열 데이터(주식, 부동산, 지지율, 원자재 가격, 미세먼지 농도, ...)를 시각화.
```python
# 선 그래프를 그리기 위한 가상 데이터
np.random.seed(1)
df = pd.DataFrame(data = {
      'day': np.arange(1, 11),
      'price': np.random.rand(10)
})
df
```
![image](https://github.com/user-attachments/assets/358c6f12-17be-4331-8fcf-295b7389fc58)

```python
# plt.plot(x 좌표로 사용할 변수, y좌표로 사용할 변수, 포맷 문자열)
# 포맷 문자열: color(선과 마커의 색깔), marker(마커 모양), linestyle(선 스타일/모양) 정보를 문자열로 만듦.
#              color: r(red), g(green), b(blue), k(black), ...
#              marker: o, s, ^, v, *, +, ...
#              linestyle: -(직선, solid line), --(dashed line), :(점선, dotted line), ...
plt.plot(df.day, df.price, 'go-')  # g : 컬러(초록색), o : 마커, - : 선 스타일

plt.xlabel('day')
plt.ylabel('price')
plt.grid()

plt.show() 
```
![image](https://github.com/user-attachments/assets/6744c6a1-b34d-4de4-867f-0946a6b1f044)

```python
plt.plot(df.day, df.price, color = 'IndianRed', linestyle = 'dotted',, marker = 'v')

plt.grid()
plt.xlabel('day')
plt.ylabel('price')

plt.show()
```
![image](https://github.com/user-attachments/assets/8f756c55-c117-4789-80ff-b969c0cba4bb)

# 연습문제 1.<br>
아래의 데이터 프레임에서 samsung, hynix, nvidia 데이터를 선 그래프로 그리세요.

  - 선 그래프 3개가 함께 표현
  - 각각의 데이터는 다른 색깔, 마커, 선 스타일로 표현
  - 범례 표시
```python
np.random.seed(1)
df = pd.DataFrame({'samsung': np.random.rand(10),
                   'hynix': np.random.rand(10),
                   'nvidia': np.random.rand(10)})
```
```python
plt.plot(df.samsung, 'bo-', label= 'samsung')
plt.plot(df.hynix, 'rs:', label = 'hynix')
plt.plot(df.nvidia, 'gv--', label = 'nvidia')

plt.legend()
plt.show()
```
![image](https://github.com/user-attachments/assets/defb55b6-d9ab-4e4e-b9e1-b84af3fc735d)

# 연습문제 2.<br>

iris 데이터셋 시각화.
   - seaborn 패키지의 iris 예제 데이터셋 로드
   - DataFrame 확인(head, info, describe, value_counts 등)
   - petal_width ~ petal_length 산점도
        - species 별로 마커의 색깔을 다르게 표현

```python
iris = sns.load_dataset('iris')
iris.info()
```
![image](https://github.com/user-attachments/assets/2f90bcfd-0a69-4d97-aa83-cad01de8a2c6)

```python
# 연속형 변수들의 기술 통계량
iris.describe()
```
![image](https://github.com/user-attachments/assets/678af524-32ae-4f4b-aa36-a9938d5510e9)

```python
# 카테고리 타입 빈도수
iris.species.value_counts()
```
![image](https://github.com/user-attachments/assets/43b722c1-999c-43d1-851e-4f348120ee3f)

```python
setosa = iris[iris.species == 'setosa']
versicolor = iris[iris.species == 'versicolor']
virginica = iris[iris.species == 'virginica']

plt.scatter(x = setosa.petal_length, y = setosa.petal_width , label= 'setosa', alpha = 0.5)
plt.scatter(x = versicolor.petal_length, y = versicolor.petal_width , label= 'versicolor', alpha = 0.5)
plt.scatter(x = virginica.petal_length, y = virginica.petal_width , label= 'virginica', alpha = 0.5)

plt.legend()
plt.xlabel('petal length(cm)')
plt.ylabel('petal width(cm)')
plt.grid()

plt.show()
```
![image](https://github.com/user-attachments/assets/338f7cca-3e5d-4ae6-b8b6-d0e8fbdd1b06)

```python
species = iris.species.unique()
for s in species:
    subset_df = iris[iris.species == s]
    plt.scatter(x = subset_df.petal_length, y = subset_df.petal_width, label = s, alpha = 0.5)

plt.legend()
plt.xlabel('petal length(cm)')
plt.ylabel('petal width(cm)')
plt.grid()
plt.show()
```
![image](https://github.com/user-attachments/assets/c1f0a13a-fb75-46ad-967f-97dec601a617)

# 필요한 모듈 import
```python
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns
```

# 데이터셋 준비
```python
tips = sns.load_dataset('tips')
tips.info()
```
![image](https://github.com/user-attachments/assets/0ef64757-93bc-4488-8580-5842a725cae2)

# Box Plot
```python
tips.total_bill.describe()
```
![image](https://github.com/user-attachments/assets/7a4c6346-5286-4141-a013-8925256732d3)

```python
sns.boxplot(data = tips, y = 'total_bill')
plt.show()
```
![image](https://github.com/user-attachments/assets/bc563780-5b1d-4bfd-8ef9-b793b062be76)

```python
sns.boxplot(data = tips, x = 'total_bill')
plt.grid()
plt.show()
```
![image](https://github.com/user-attachments/assets/38a5a5d0-e0c3-495c-a592-39e8f058eded)

## 성별 팁의 box plot
```python
sns.boxplot(data = tips, x = 'sex', y = 'tip', hue = 'sex')  # hue -> 카테고리 별로 색을 다르게 하고 싶을 때(성별로 색깔 다르게)
plt.show()
```
![image](https://github.com/user-attachments/assets/a48e1e2a-fa50-47c2-920b-10cd6141481e)

```python
tips[tips.sex == 'Male'].tip.describe()
```
![image](https://github.com/user-attachments/assets/4b1c2a97-30bc-45f5-8fd1-995d70048ad3)

```python
tips[tips.sex == 'Female'].tip.describe()
```
![image](https://github.com/user-attachments/assets/e688ba4e-397c-4c22-927e-fd48d93925de)

## 흡연 여부별 팁의 box plot
```python
sns.boxplot(data = tips, x = 'smoker', y = 'tip')
plt.grid()
plt.show()
```
![image](https://github.com/user-attachments/assets/2c0d8f55-72f2-47b5-bc2d-f1477562adff)

## 요일별 팁의 box plot
```python
sns.boxplot(data = tips, x = 'day', y = 'tip')
plt.grid()
plt.show()
```
![image](https://github.com/user-attachments/assets/04009073-0ab6-4a2c-a319-5d15fd25edb4)

## 시간별 팁의 box plot
```python
sns.boxplot(data = tips, x = 'time', y = 'tip')
plt.grid()
plt.show()
```
![image](https://github.com/user-attachments/assets/66dba409-fd74-4f4f-a17c-928af07cf3d3)

## 요일별, 성별 팁의 box plot
```python
sns.boxplot(data = tips, x = 'day', y = 'tip', hue = 'sex')
plt.grid()
plt.show()
```
![image](https://github.com/user-attachments/assets/8d0b0e76-c574-4d09-8f8a-080f1ccd6f2c)

```python
sns.boxplot(data = tips, x = 'sex', y = 'tip', hue = 'day')
plt.show()
```
![image](https://github.com/user-attachments/assets/cfda4f7f-80d5-41d5-ad36-584713a9e57d)

## 2개 이상의 Axes 사용하기
```
Figure에 Axes(nrows = 1, ncol = 2) 2개를 생성하고, 성별 tip의 box plot과 시간별 tip의 box plot을 그리세요.
```
```python
fig, axes = plt.subplots(ncols = 2, figsize = (8, 4))

sns.boxplot(data = tips, x = 'sex', y = 'tip', ax = axes[0])
sns.boxplot(data = tips, x = 'time', y = 'tip', ax = axes[1])

plt.show()
```
![image](https://github.com/user-attachments/assets/501efaa1-38df-4197-a832-f4bb4b27b38c)

# Histogram(히스토그램)
```python
sns.histplot(data = tips, x = 'total_bill', bins = 10)

plt.show()
```
![image](https://github.com/user-attachments/assets/afca6b68-9596-40b5-b802-b1f5ae43c337)

```python
sns.histplot(data = tips, y = 'total_bill')
plt.show()
```
![image](https://github.com/user-attachments/assets/315b4b2c-c893-419f-8f26-2e9bb68db8ed)

<br>

Figure에 Axes(nrows = 2, ncols = 1) 2개를 생성, 위쪽에는 tip의 box plot, 아래쪽에는 tip의 histogram을 그리세요.

```python
fig, axes = plt.subplots(nrows = 2, figsize = (4, 8))

sns.boxplot(data = tips, x = 'tip', ax = axes[0])
sns.histplot(data = tips, x = 'tip', ax = axes[1])

plt.show()
```
![image](https://github.com/user-attachments/assets/a982f076-028b-415c-8bc9-159613b77e3e)


# 막대 그래프
## countplot()
<br>
카테고리(범주) 타입 변수의 빈도수를 막대로 시각화.

```python
tips.sex.value_counts()
```
![image](https://github.com/user-attachments/assets/5cdb5cbc-09f5-40eb-8d23-521b87bda314)

```python
sns.countplot(data = tips, x = 'sex')
plt.show()
```
![image](https://github.com/user-attachments/assets/b05429fe-c94b-44de-a62f-bbab9a5ba743)

```python
sns.countplot(data = tips, y = 'sex')
plt.show()
```
![image](https://github.com/user-attachments/assets/2682bc2f-1fe3-46a3-acf6-8a790b7f15c2)

<br>
요일별 빈도수 막대 그래프

```python
sns.countplot(data = tips, x = 'day', order = tips.day.value_counts().index)
plt.show()
```
![image](https://github.com/user-attachments/assets/6ee19272-fbcc-40fa-8445-d867983da063)

<br>
요일별 성별 빈도수 막대 그래프

```python
sns.countplot(data = tips, x = 'day', hue = 'sex')
plt.show()
```
![image](https://github.com/user-attachments/assets/0feb8c2e-ae4a-439d-beb4-b6786b70ee7b)

```python
# days = tips.day.cat.categories
# for d in days:
#     print(f'--- {d} ---')
#     subset = tips[tips.day == d]
#     print(subset.sex.value_counts())

# data_frame.groupby([cat1, cat2, ...]).column_name.stat_func()
tips.groupby(by = ['day', 'sex']).sex.count()   # 요일과 성별로 묶고 성별의 갯수를 세는거
```
![image](https://github.com/user-attachments/assets/92aeca7a-f2de-4ed5-bbc1-562edfbda04e)

```python
sns.countplot(data = tips, x = 'sex', hue = 'day')
plt.show()
```
![image](https://github.com/user-attachments/assets/5f4aaca2-e1c3-4ff3-934e-4f9238c81fdb)

```python
tips.groupby(by = ['sex', 'day']).day.count()   # 요일에서 count
```
![image](https://github.com/user-attachments/assets/61536547-5b9f-4a57-ae49-12fea9f4cdb2)

<br>
성별 흡연여부별 빈도수 막대 그래프

```python
sns.countplot(data = tips, x = 'sex', hue = 'smoker')   # y값은 지정 안해줘도 자동으로 count
plt.show()
```
![image](https://github.com/user-attachments/assets/0dd79402-c6b6-44c9-9edb-ffff9d58072e)

<br>

```python
tips.groupby(by = ['sex', 'smoker']).smoker.count()
```
![image](https://github.com/user-attachments/assets/23f81d9e-16f3-429b-817a-26d41d0f6da9)

```python
fig, ax = plt.subplots()    # 아규먼트 안주면 행, 열 1개
sns.countplot(data = tips, x = 'day')
for p in ax.patches:
    # print(p)
    ax.text(x = p.get_x() + p.get_width() / 2,      # 텍스트를 그릴 x 좌표
            y = p.get_y() + p.get_height() + 0.5,   # 텍스트를 그릴 y 좌표
            s = f'{p.get_height():.0f}',            # 표시할 텍스트 내용
            ha = 'center',                          # horizontal alignment(수평(좌우)정렬)
            fontweight = 'bold')                    # font weight: light, regular, bold, ...
plt.show()
```
![image](https://github.com/user-attachments/assets/266798db-cd90-4ff5-8a14-9201b27d570a)

## barplot()<br>
- 그룹별 변수(숫자 타입)의 기술 통계량(예: 평균, 최댓값, ...)을 막대로 시각화.<br>
- 카테고리(범주) 타입 변수와 연속형 변수를 함께 시각화.
<br>
성별 팁의 평균
```python
tips.groupby(by = ['sex']).tip.mean()
```
![image](https://github.com/user-attachments/assets/9866fa8b-3b9a-4737-9be5-7ac7303fcbf0)

```python
sns.barplot(data = tips, x = 'sex', y = 'tip', errorbar = None)
# estimator = 'mean' 기본값이기 때문에 막대의 높이(height)는 평균값.
plt.show()
```
![image](https://github.com/user-attachments/assets/a1a3dacb-e438-472e-877c-304fc621594b)

<br>
성별 팁의 중위값(50%)

```python
tips.groupby(by = ['sex']).tip.median()
```
![image](https://github.com/user-attachments/assets/2378a489-f235-49ab-963a-831ef896bbc1)

```python
sns.barplot(data = tips, x = 'sex', y = 'tip', estimator = 'median', errorbar = None)
plt.show()
```
![image](https://github.com/user-attachments/assets/85b94e0e-42d0-49fc-9e7e-6706d12d4e47)

<br>
성별 팁의 최댓값
```python
tips.groupby(by = ['sex']).tip.max()
```
![image](https://github.com/user-attachments/assets/dd2ceda4-7f50-4bc5-9aa1-c838884f435b)

```python
sns.barplot(data = tips, x = 'sex', y = 'tip', estimator = 'max', errorbar = None)
plt.show()
```
![image](https://github.com/user-attachments/assets/8f8bb11d-61ea-420f-ba94-2a4547e312ac)

<br>
요일별 팁의 평균 가로 막대 그래프

```python
tips.groupby(by = ['day']).tip.mean()
```
![image](https://github.com/user-attachments/assets/f0919ad4-10f2-444f-bdb4-27d5599ce9aa)

```python
sns.barplot(data = tips,x = 'tip', y = 'day', errorbar = None)  # estimator는 기본값이 평균
plt.show()
```
![image](https://github.com/user-attachments/assets/737f3f2b-ab24-43cd-b2ad-83bf844121f8)

<br>
요일별 성별 팁의 평균 세로 막대 그래프

```python
tips.groupby(by = ['day', 'sex']).tip.mean()
```
![image](https://github.com/user-attachments/assets/57eb37cc-9fe6-4ed0-b1bb-d04be82f7a1e)

```python
sns.barplot(data = tips, x = 'day', y = 'tip', hue = 'sex', errorbar = None)
plt.show()
```
![image](https://github.com/user-attachments/assets/4ba530ec-ee02-4d36-9fd7-713a9c2da2ba)

```python
sns.barplot(data = tips, x = 'sex', y = 'tip', hue = 'day', errorbar = None)
plt.show()
```
![image](https://github.com/user-attachments/assets/fb86734d-f410-4d98-a30e-3d83901d52f0)
